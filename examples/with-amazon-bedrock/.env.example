# AWS Configuration for Amazon Bedrock
# VoltAgent uses the standard AWS credential chain. Provide AWS_REGION and either
# access keys, a profile, or an IAM role.

# Region where Bedrock is available (us-east-1, us-west-2, eu-central-1, etc.)
AWS_REGION=us-east-1

# Option 1: Use AWS Access Keys
# Uncomment and fill in your credentials:
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key
# AWS_SESSION_TOKEN=your-session-token  # Optional for temporary credentials

# Option 2: Use AWS Profile (recommended for local development)
# First configure your profile: aws configure --profile your-profile-name
# Then uncomment and set:
# AWS_PROFILE=your-profile-name

# Option 3: Use IAM Role (automatic when running on AWS infrastructure)
# No additional configuration needed - credentials are automatically obtained
