export default function Home() {
  return (
    <main>
      <h1>VoltAgent + Chat SDK (Slack)</h1>
      <p>
        This example exposes a webhook endpoint at <code>/api/webhooks/slack</code>.
      </p>
      <p>
        Configure your Slack app request URL to point to this endpoint, then mention your bot in a
        channel.
      </p>
    </main>
  );
}
